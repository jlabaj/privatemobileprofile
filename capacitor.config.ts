import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jakublabaj.com.app',
  appName: 'jakubs-profile',
  webDir: 'dist/apps/jakubs-profile/browser'
};

export default config;
